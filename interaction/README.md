# Interaction Module

Text/command mapping and task dispatch currently live in `main.py`.

The interaction flow maps recognized voice commands to fruit targets such as apple, orange, and pear, then forwards the selected target to the visual detector and robotic-arm control logic.
