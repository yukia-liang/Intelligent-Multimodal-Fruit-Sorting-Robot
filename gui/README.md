# GUI Module

The current GUI is implemented directly in `main.py` through Tkinter.

It provides real-time camera visualization, voice-command status, system logs, detection state, and robotic-arm operation feedback.

This folder is reserved for future extraction of the GUI into a standalone module.
